#include <stdio.h>

struct node
{
    int n;
    struct node *next;
};
struct node *head=NULL;
int main()
{
    struct node *new_node;
    new_node = (struct node *)malloc(sizeof(struct node));
    scanf("%d", &new_node->n);
    int option;
    


}
